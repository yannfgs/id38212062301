// Um array é uma estrutura de dados que armazena uma coleção de elementos. Esses elementos podem ser de qualquer tipo, incluindo números, strings e outros objetos.
// A contagem de arrays se inicia no 0, portanto a posição 1 do array é o segundo elemento.


let numeros = [1, 2, 3, 4, 5];
console.log(numeros[0]); // 1

let misto = [1, "dois", false, { nome: "João" }, [5, 6, 7]];
console.log(misto[3].nome); // João

